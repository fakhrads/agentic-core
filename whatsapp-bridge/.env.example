# Copy to .env in this folder. BRIDGE_SECRET must match AGENT_WHATSAPP_BRIDGE_SECRET
# in the main repo's .env — `agent setup` prints the value it wrote.
BRIDGE_PORT=8098
BRIDGE_SECRET=
AGENT_WEBHOOK_URL=http://127.0.0.1:8099/webhooks/whatsapp
